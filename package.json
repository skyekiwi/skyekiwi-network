{
  "name": "skyekiwi-protocol-tee",
  "version": "0.1.0",
  "main": "index.js",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "enclave:build": "node ./scripts/enclave-build.cjs",
    "enclave:run": "node ./scripts/enclave-run.cjs",
    "enclave:start": "yarn enclave:build && yarn enclave:run",
    "enclave:sim": "node ./scripts/enclave-sim.cjs",
    "enclave:ci": "node ./scripts/enclave-ci.cjs",

    "blockchain:build": "node ./scripts/blockchain-build.cjs",
    "blockchain:ci": "node ./scripts/blockchain-ci.cjs",
    "blockchain:test": "node ./scripts/blockchain-test.cjs",

    "contract-sdk:test": "node ./scripts/contract-sdk-test.cjs",

    "contract:compile": "node ./scripts/contract-compile.cjs",
    "mock-enclave:start": "yarn contract:compile && npx ts-node mock-enclave/host/index.ts",
    "mock-enclave:vm": "cd mock-enclave && yarn start",
    "mock-enclave:test": "cd mock-enclave && yarn test",

    "chaos:party": "cd chaos && yarn party"
  }
}
